# broker-robinhood (future extension — DISABLED)

Robinhood connector. **Disabled stub only.** Implemented as `RobinhoodBroker` in
[`apps/api/tradepilot/broker/base.py`](../../apps/api/tradepilot/broker/base.py).

⚠️ **No unofficial / private Robinhood APIs are used or supported.** If a real integration is
ever added, it must use only officially documented APIs and remain disabled by default.
